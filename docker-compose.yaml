services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME:-local}/system-metrics-agent:latest
    container_name: metrics-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - metrics-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  agent:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME:-local}/system-metrics-agent:latest
    container_name: metrics-agent
    env_file:
      - .env
    depends_on:
      api:
        condition: service_healthy
    networks:
      - metrics-network
    command: ["python", "-m", "app.agent"]

networks:
  metrics-network:
    driver: bridge
